# تغییرات این نسخه

## ۱) رفع باگ «لینک ساب باز نمی‌شود»
ریشه‌ی باگ: یک متغیر سراسری (`CONFIG["host"]`) با **هر** درخواستی که به سرور می‌رسید (حتی هلث‌چک، اسکنر، یا هر
درخواست با هدر Host نامعتبر) بازنویسی می‌شد. در نتیجه لینک‌های ساب بعدی — به‌خصوص آن‌هایی که ربات فروش می‌ساخت —
می‌توانستند با دامنه/آدرس اشتباه ساخته شوند.

رفع شد با:
- افزودن تنظیم «آدرس عمومی پنل» (Public Base URL) در تب تنظیمات — وقتی ثابت شود، همیشه همان استفاده می‌شود.
- حذف کامل رفتار mutation سراسری (دیگر هیچ درخواستی نمی‌تواند لینک بقیه را خراب کند).
- اصلاح scheme (http/https) بر اساس همان تنظیم، به‌جای فرض ثابت https.

**پیشنهاد:** بعد از آپلود، حتماً از تب تنظیمات، آدرس واقعی دامنه‌ی پنل خود را وارد و ذخیره کنید.

## ۲) اجرای ربات فروش از داخل پنل (بدون نیاز به .env)
- تب «تنظیمات» → بخش «ربات فروش تلگرام»: توکن و آیدی ادمین‌های مجاز را وارد و ذخیره کنید.
- دکمه‌های شروع/توقف ربات مستقیماً از پنل.
- توکن و تنظیمات در فایل state ذخیره می‌شوند، پس با ری‌استارت سرویس هم از بین نمی‌روند.

## ۳) داشبورد جدید (ساختار متفاوت)
- ناوبری با تب‌های بالای صفحه به‌جای سایدبار
- جداول داده‌ی واقعی به‌جای کارت‌های شیشه‌ای
- فرم‌های ساخت/ویرایش در کشوی کناری (Drawer)
- تمام صفحات: داشبورد، کانفیگ‌ها، دسته‌بندی‌ها، گروه‌های ساب، پلن‌های فروش، گزارش‌ها، ادمین‌ها، فعالیت‌ها، تنظیمات

⚠️ داشبورد فقط **رابط کاربری (pages.py)** را عوض کرده و از همان APIهای موجود در `main.py` استفاده می‌کند —
منطق بک‌اندی relay/xhttp/پرداخت دست‌نخورده مانده تا ریسک قطع سرویس فعلی شما صفر باشد.

## ۴) پروتکل VLESS-TCP (واقعی، نه فقط دکوری)
یک پروتکل جدید «VLESS TCP (خام)» اضافه شد که واقعاً روی سرور کار می‌کند — یک سرویس‌دهنده‌ی
TCP خام و مستقل (`tcp_relay.py`) که روی پورت جداگانه‌ای (پیش‌فرض `6543`، با متغیر محیطی
`TCP_LISTEN_PORT` قابل تغییر) بالا می‌آید و کاملاً مستقل از پورت اصلی HTTP پنل عمل می‌کند.

**نکته‌ی مهم و صادقانه درباره‌ی پروتکل‌های دیگر:** در پروژه‌ی اصلی چند پروتکل دیگر هم در
لیست بودند (Shadowsocks، SOCKS5، HTTP Proxy، Hysteria2، TUIC، WireGuard) که فقط لینک برایشان
ساخته می‌شد ولی **هیچ سرویس‌دهنده‌ی واقعی روی سرور نداشتند** — این از قبل در پروژه وجود
داشت. حالا در `/api/protocols` و در dropdown ساخت کانفیگ، این‌ها با ⚠️ علامت‌گذاری شده‌اند تا
اشتباهی برای مشتری فروخته نشوند؛ پروتکل‌های واقعی با ✅ مشخص شده‌اند.

**برای کار کردن روی Railway:** چون Railway فقط یک پورت HTTP عمومی expose می‌کند، برای
دسترسی بیرونی به پورت TCP باید از قابلیت **TCP Proxy** ریلوی (تب Networking سرویس) استفاده
کنی. ریلوی یک آدرس و پورت عمومی می‌دهد — همان‌ها را در تب «تنظیمات → آدرس/پورت عمومی TCP»
پنل وارد کن. این نسخه‌ی TCP بدون TLS (security=none) کار می‌کند؛ برای رمزنگاری واقعی، لایه‌ی
TLS جداگانه (مثلاً stunnel) جلوش لازم است که فعلاً پیاده‌سازی نشده.

## ۵) طراحی و برند
- لوگوی خودتان (ماسک بنفش VodiWalker) در صفحه‌ی ورود (به‌صورت تصویر پس‌زمینه) و در نوار
  بالای داشبورد جایگزین شد.
- پالت رنگی از آبی به بنفش برند شما (`#9455ff`) تغییر کرد.
- در dropdown انتخاب پروتکل، گزینه‌های واقعاً کارکننده با ✅ و غیرفعال‌ها با ⚠️ مشخص شدند.

## فاز بعدی (در صورت تمایل)
تفکیک `main.py` (حدود ۶۵۰۰ خط) به ماژول‌های جدا (auth, links, subs, categories, admins, ...) —
این کار را به‌صورت جداگانه و با تست مرحله‌به‌مرحله انجام می‌دهیم تا سرویس زنده‌ی شما خراب نشود.
همچنین در صورت نیاز، افزودن TLS واقعی به VLESS-TCP.

## بروزرسانی جدید: داشبورد سایدباری، دو زبانه، هندشیک HTTPUpgrade واقعی

- **ساختار داشبورد**: از تب‌های بالای صفحه به یک سایدبار عمودی (شبیه پنل‌های 3x-ui/Sanaei) تغییر کرد. تمام صفحات قبلی حفظ شدند.
- **دو زبانه (فارسی/انگلیسی)**: دکمه تعویض زبان در پایین سایدبار و بالای فرم ورود. ناوبری، عنوان صفحات، دکمه‌ها و سرستون جدول‌ها به هر دو زبان برگردانده شده‌اند. متن‌هایی که مستقیماً از سرور می‌آیند (پیام‌های فعالیت، توست‌ها، برچسب وضعیت داخل جدول‌ها) فعلاً همچنان فارسی هستند — اگر لازم است این بخش‌ها هم ترجمه شوند، در یک مرحله‌ی جدا انجام می‌شود.
- **صفحه‌ی ورود**: بازطراحی شد؛ دکمه‌ی زبان اضافه شد. رمز/نام کاربری همان مکانیزم قبلی (`/login` + `/api/login`) را استفاده می‌کند، فقط ظاهر عوض شده.
- **پروتکل جدید واقعی — VLESS HTTPUpgrade** (`relay_httpupgrade.py`): برخلاف پروتکل‌های دموی قبلی، این یکی هندشیک واقعی HTTP Upgrade را پیاده می‌کند و در `/api/protocols` با ✅ مشخص می‌شود.
  ⚠️ **نکته‌ی مهم:** چون ASGI/uvicorn از هندشیک Upgrade عمومی (غیر از وب‌سوکت استاندارد) پشتیبانی نمی‌کند، این پروتکل هم — دقیقاً مثل vless-tcp — روی یک **پورت TCP جدا** سرو می‌شود (پیش‌فرض `6544`، با env var `HTTPUPGRADE_LISTEN_PORT` قابل تغییر). روی Railway باید یک TCP Proxy جداگانه برای همین پورت فعال کنی و آدرس/پورت عمومی‌اش را در تب «تنظیمات» پنل وارد کنی (فیلد جدید مخصوص HTTPUpgrade).
- بقیه‌ی پروتکل‌های دموی قبلی (Shadowsocks, SOCKS5, HTTP, Hysteria2, TUIC, WireGuard) دست‌نخورده و همچنان با ⚠️ فقط لینک می‌سازند، سرویس‌دهنده‌ی واقعی روی سرور ندارند.
